import { CardLast4Pipe } from './card-last4.pipe';

describe('CardLast4Pipe', () => {
  it('create an instance', () => {
    const pipe = new CardLast4Pipe();
    expect(pipe).toBeTruthy();
  });
});
