import { UrlNameIdPipe } from './url-name-id.pipe';

describe('UrlNameIdPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlNameIdPipe();
    expect(pipe).toBeTruthy();
  });
});
